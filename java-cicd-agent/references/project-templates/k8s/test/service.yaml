apiVersion: v1
kind: Service
metadata:
  name: demo-service
  namespace: test
spec:
  selector:
    app: demo-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
