# Argo CD 环境模式

## 测试环境

- 应用名：`<service>-test`
- 可使用自动同步
- 反馈回路最快

## 预发布环境

- 应用名：`<service>-staging`
- 可以要求人工晋级
- 更接近生产环境验证

## 生产环境

- 应用名：`<service>-prod`
- 审批路径更严格
- 不允许在 Git 之外随意自动修改配置

## 建议

尽量让各环境的 Argo CD Application 命名和路径模式保持对称，这样运维人员更容易快速判断名称与位置。
